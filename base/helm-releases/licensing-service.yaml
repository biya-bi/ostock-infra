apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: licensing-service
  namespace: ostock
spec:
  interval: 10m
  chart:
    spec:
      chart: ./charts/licensing-service
      sourceRef:
        kind: GitRepository
        name: ostock-helm
      interval: 10m
      reconcileStrategy: Revision
      valuesFiles:
        - ./charts/licensing-service/values.yaml
