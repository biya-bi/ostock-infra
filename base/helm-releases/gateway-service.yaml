apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gateway-service
  namespace: ostock
spec:
  interval: 10m
  chart:
    spec:
      chart: ./charts/gateway-service
      sourceRef:
        kind: GitRepository
        name: ostock-helm
      interval: 10m
      valuesFiles:
        - ./charts/gateway-service/values.yaml
