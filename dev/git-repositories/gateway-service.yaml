apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gateway-service
  namespace: ostock
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: ssh-rsa-credentials
  url: ssh://github.com/biya-bi/ostock-gateway-service.git
